{
    "name": "your-name/push-notification-project",
    "description": "Push notification system for IP monitoring",
    "type": "project",
    "require": {
        "minishlink/web-push": "^6.0",
        "php": "^7.4 || ^8.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    },
    "minimum-stability": "stable",
    "license": "MIT"
}
